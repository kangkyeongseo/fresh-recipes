mixin ingredient(ingredient)
    a(href=`/ingredient/${ingredient._id}`)
        div
            span=ingredient.name 
        div
            span=ingredient.amount
            span=ingredient.amountType
        div
            span 남은기간
            span #{Math.round((new Date(ingredient.periodLife) - new Date())/1000/3600/24)}일
        