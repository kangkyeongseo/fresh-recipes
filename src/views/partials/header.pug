haeder
    ul  
        li 
            i(class="fas fa-leaf")
            span Fresh Recipes
        Li 
            if !loggedIn
                a(href="/login") 로그인
                a(href="/join") 회원가입
            if loggedIn
                a(href="/logout") 로그아웃